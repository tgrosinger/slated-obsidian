// TODO

describe('findMomentForDailyNote', () => {
  test('it should return an error if the file is not for a date', () => {});

  test('it should return a moment when the file matches the format', () => {});
});
